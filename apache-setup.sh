step1 -check if apache is installed in our system
   if YES 
         continue
    if not 
        install apache
step 2: setup apache
        enable apache
        check if apache is runnig
  if status is runnig
        print "apache status active"
   if status not running 
         print " apache failure 